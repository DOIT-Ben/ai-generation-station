const fs = require('fs');
const path = require('path');
const assert = require('assert');
const { CSS_MODULES } = require('./test-css-utils');

function main() {
  const html = fs.readFileSync(path.join(__dirname, 'public', 'index.html'), 'utf8');
  const authHtml = fs.readFileSync(path.join(__dirname, 'public', 'auth', 'index.html'), 'utf8');
  const loginHtml = fs.readFileSync(path.join(__dirname, 'public', 'login', 'index.html'), 'utf8');
  const registerHtml = fs.readFileSync(path.join(__dirname, 'public', 'register', 'index.html'), 'utf8');
  const accountHtml = fs.readFileSync(path.join(__dirname, 'public', 'account', 'index.html'), 'utf8');
  const adminHtml = fs.readFileSync(path.join(__dirname, 'public', 'admin', 'index.html'), 'utf8');
  const appJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'app.js'), 'utf8');
  const authPageJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'auth-page.js'), 'utf8');
  const accountPageJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'account-page.js'), 'utf8');
  const adminPageJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'admin-page.js'), 'utf8');
  const siteShellJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'site-shell.js'), 'utf8');
  const chatModelUtilsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'chat-model-utils.js'), 'utf8');
  const workspaceChatModelToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'workspace-chat-model-tools.js'), 'utf8');
  const chatMarkdownJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'chat-markdown.js'), 'utf8');
  const chatMessageMetaToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'chat-message-meta-tools.js'), 'utf8');
  const chatMessageNodeToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'chat-message-node-tools.js'), 'utf8');
  const chatFailureToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'chat-failure-tools.js'), 'utf8');
  const chatEntryToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'chat-entry-tools.js'), 'utf8');
  const chatMessageActionToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'chat-message-action-tools.js'), 'utf8');
  const chatComposerToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'chat-composer-tools.js'), 'utf8');
  const chatStreamToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'chat-stream-tools.js'), 'utf8');
  const chatSendToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'chat-send-tools.js'), 'utf8');
  const chatRenderRuntimeToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'chat-render-runtime-tools.js'), 'utf8');
  const templateToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'template-tools.js'), 'utf8');
  const workspaceTemplateToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'workspace-template-tools.js'), 'utf8');
  const workspaceGenerationToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'workspace-generation-tools.js'), 'utf8');
  const workspaceMediaToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'workspace-media-tools.js'), 'utf8');
  const workspaceConversationToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'workspace-conversation-tools.js'), 'utf8');
  const workspaceStateToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'workspace-state-tools.js'), 'utf8');
  const workspaceShellToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'workspace-shell-tools.js'), 'utf8');
  const workspaceUiToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'workspace-ui-tools.js'), 'utf8');
  const workspacePageInitToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'workspace-page-init-tools.js'), 'utf8');
  const workspaceAuthToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'workspace-auth-tools.js'), 'utf8');
  const workspaceInitToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'workspace-init-tools.js'), 'utf8');
  const conversationWorkflowToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'conversation-workflow-tools.js'), 'utf8');
  const conversationListToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'conversation-list-tools.js'), 'utf8');
  const conversationActionToolsJs = fs.readFileSync(path.join(__dirname, 'public', 'js', 'conversation-action-tools.js'), 'utf8');

  assert.ok(html.includes('/js/app-shell.js'), 'index should load app-shell.js before app.js');
  assert.ok(html.includes('/js/site-shell.js'), 'index should load site-shell.js before app.js for queued auth feedback');
  assert.ok(html.includes('/js/chat-model-utils.js'), 'index should load the chat model utility module before app.js');
  assert.ok(html.includes('/js/workspace-chat-model-tools.js'), 'index should load the workspace chat-model utility module before app.js');
  assert.ok(html.includes('/js/chat-markdown.js'), 'index should load the chat markdown utility module before app.js');
  assert.ok(!html.includes('/js/chat-excerpt-tools.js'), 'index should not load the removed chat excerpt utility module');
  assert.ok(html.includes('/js/chat-message-meta-tools.js'), 'index should load the chat message meta utility module before app.js');
  assert.ok(html.includes('/js/chat-message-node-tools.js'), 'index should load the chat message node utility module before app.js');
  assert.ok(html.includes('/js/chat-failure-tools.js'), 'index should load the chat failure utility module before app.js');
  assert.ok(html.includes('/js/chat-entry-tools.js'), 'index should load the chat entry utility module before app.js');
  assert.ok(html.includes('/js/chat-message-action-tools.js'), 'index should load the chat message action utility module before app.js');
  assert.ok(html.includes('/js/chat-composer-tools.js'), 'index should load the chat composer utility module before app.js');
  assert.ok(html.includes('/js/chat-stream-tools.js'), 'index should load the chat stream utility module before app.js');
  assert.ok(html.includes('/js/chat-send-tools.js'), 'index should load the chat send utility module before app.js');
  assert.ok(html.includes('/js/chat-render-runtime-tools.js'), 'index should load the chat render runtime utility module before app.js');
  assert.ok(!html.includes('/js/chat-outline-tools.js'), 'index should not load the removed chat outline utility module');
  assert.ok(html.includes('/js/template-tools.js'), 'index should load the template utility module before app.js');
  assert.ok(html.includes('/js/workspace-template-tools.js'), 'index should load the workspace template utility module before app.js');
  assert.ok(html.includes('/js/workspace-generation-tools.js'), 'index should load the workspace generation utility module before app.js');
  assert.ok(html.includes('/js/workspace-media-tools.js'), 'index should load the workspace media utility module before app.js');
  assert.ok(html.includes('/js/workspace-conversation-tools.js'), 'index should load the workspace conversation utility module before app.js');
  assert.ok(html.includes('/js/workspace-state-tools.js'), 'index should load the workspace state utility module before app.js');
  assert.ok(html.includes('/js/workspace-shell-tools.js'), 'index should load the workspace shell utility module before app.js');
  assert.ok(html.includes('/js/workspace-ui-tools.js'), 'index should load the workspace ui utility module before app.js');
  assert.ok(html.includes('/js/workspace-page-init-tools.js'), 'index should load the workspace page-init utility module before app.js');
  assert.ok(html.includes('/js/workspace-auth-tools.js'), 'index should load the workspace auth utility module before app.js');
  assert.ok(html.includes('/js/workspace-init-tools.js'), 'index should load the workspace init utility module before app.js');
  assert.ok(html.includes('/js/conversation-workflow-tools.js'), 'index should load the conversation workflow utility module before app.js');
  assert.ok(html.includes('/js/conversation-list-tools.js'), 'index should load the conversation list utility module before app.js');
  assert.ok(html.includes('/js/conversation-action-tools.js'), 'index should load the conversation action utility module before app.js');
  const toastLiveRegionMarkup = 'id="toast-container" class="toast-container" role="status" aria-live="polite" aria-atomic="true"';
  [html, authHtml, loginHtml, registerHtml, accountHtml, adminHtml].forEach((pageHtml, index) => {
    assert.ok(pageHtml.includes(toastLiveRegionMarkup), `page ${index} should expose toast container as a polite live region`);
  });
  assert.ok(appJs.includes('if (!container) return;'), 'workspace toast should tolerate a missing toast container');
  assert.ok(siteShellJs.includes('if (!container) return;'), 'site shell toast should tolerate a missing toast container');
  assert.ok(appJs.includes("toast.setAttribute('role', type === 'error' ? 'alert' : 'status');"), 'workspace toast should expose semantic status roles');
  assert.ok(siteShellJs.includes("toast.setAttribute('role', type === 'error' ? 'alert' : 'status');"), 'site shell toast should expose semantic status roles');
  assert.ok(appJs.includes("toast.setAttribute('aria-live', type === 'error' ? 'assertive' : 'polite');"), 'workspace toast should announce errors assertively and other messages politely');
  assert.ok(siteShellJs.includes("toast.setAttribute('aria-live', type === 'error' ? 'assertive' : 'polite');"), 'site shell toast should announce errors assertively and other messages politely');
  assert.ok(chatModelUtilsJs.includes('AigsChatModelUtils'), 'chat model utilities should publish a dedicated browser module');
  assert.ok(workspaceChatModelToolsJs.includes('AigsWorkspaceChatModelTools'), 'workspace chat-model utilities should publish a dedicated browser module');
  assert.ok(chatMarkdownJs.includes('AigsChatMarkdown'), 'chat markdown utilities should publish a dedicated browser module');
  assert.ok(chatMessageMetaToolsJs.includes('AigsChatMessageMetaTools'), 'chat message meta utilities should publish a dedicated browser module');
  assert.ok(chatMessageNodeToolsJs.includes('AigsChatMessageNodeTools'), 'chat message node utilities should publish a dedicated browser module');
  assert.ok(chatFailureToolsJs.includes('AigsChatFailureTools'), 'chat failure utilities should publish a dedicated browser module');
  assert.ok(chatEntryToolsJs.includes('AigsChatEntryTools'), 'chat entry utilities should publish a dedicated browser module');
  assert.ok(chatMessageActionToolsJs.includes('AigsChatMessageActionTools'), 'chat message action utilities should publish a dedicated browser module');
  assert.ok(chatComposerToolsJs.includes('AigsChatComposerTools'), 'chat composer utilities should publish a dedicated browser module');
  assert.ok(chatStreamToolsJs.includes('AigsChatStreamTools'), 'chat stream utilities should publish a dedicated browser module');
  assert.ok(chatSendToolsJs.includes('AigsChatSendTools'), 'chat send utilities should publish a dedicated browser module');
  assert.ok(chatRenderRuntimeToolsJs.includes('AigsChatRenderRuntimeTools'), 'chat render runtime utilities should publish a dedicated browser module');
  assert.ok(templateToolsJs.includes('AigsTemplateTools'), 'template utilities should publish a dedicated browser module');
  assert.ok(workspaceTemplateToolsJs.includes('AigsWorkspaceTemplateTools'), 'workspace template utilities should publish a dedicated browser module');
  assert.ok(workspaceGenerationToolsJs.includes('AigsWorkspaceGenerationTools'), 'workspace generation utilities should publish a dedicated browser module');
  assert.ok(workspaceMediaToolsJs.includes('AigsWorkspaceMediaTools'), 'workspace media utilities should publish a dedicated browser module');
  assert.ok(workspaceConversationToolsJs.includes('AigsWorkspaceConversationTools'), 'workspace conversation utilities should publish a dedicated browser module');
  assert.ok(workspaceStateToolsJs.includes('AigsWorkspaceStateTools'), 'workspace state utilities should publish a dedicated browser module');
  assert.ok(workspaceShellToolsJs.includes('AigsWorkspaceShellTools'), 'workspace shell utilities should publish a dedicated browser module');
  assert.ok(workspaceUiToolsJs.includes('AigsWorkspaceUiTools'), 'workspace ui utilities should publish a dedicated browser module');
  assert.ok(workspacePageInitToolsJs.includes('AigsWorkspacePageInitTools'), 'workspace page-init utilities should publish a dedicated browser module');
  assert.ok(workspaceAuthToolsJs.includes('AigsWorkspaceAuthTools'), 'workspace auth utilities should publish a dedicated browser module');
  assert.ok(workspaceInitToolsJs.includes('AigsWorkspaceInitTools'), 'workspace init utilities should publish a dedicated browser module');
  assert.ok(conversationWorkflowToolsJs.includes('AigsConversationWorkflowTools'), 'conversation workflow utilities should publish a dedicated browser module');
  assert.ok(conversationListToolsJs.includes('AigsConversationListTools'), 'conversation list utilities should publish a dedicated browser module');
  assert.ok(conversationActionToolsJs.includes('AigsConversationActionTools'), 'conversation action utilities should publish a dedicated browser module');
  for (const cssFile of CSS_MODULES) {
    assert.ok(html.includes(`/css/${cssFile}`), `index should load modular stylesheet ${cssFile}`);
    assert.ok(authHtml.includes(`/css/${cssFile}`), `auth page should load modular stylesheet ${cssFile}`);
    assert.ok(loginHtml.includes(`/css/${cssFile}`), `login page should load modular stylesheet ${cssFile}`);
    assert.ok(registerHtml.includes(`/css/${cssFile}`), `register page should load modular stylesheet ${cssFile}`);
    assert.ok(accountHtml.includes(`/css/${cssFile}`), `account page should load modular stylesheet ${cssFile}`);
    assert.ok(adminHtml.includes(`/css/${cssFile}`), `admin page should load modular stylesheet ${cssFile}`);
  }
  assert.ok(!html.includes('/css/style.css'), 'index should not rely on the legacy monolithic stylesheet');
  assert.ok(html.includes('name="aigs-api-base-url"'), 'index should expose the optional API base-url meta tag');
  assert.ok(html.includes('/images/AG-logo.png'), 'index should expose the AG logo favicon asset');
  assert.ok(html.includes('class="logo-icon"'), 'index logo should render the AG logo image container');
  assert.ok(html.includes('class="logo-ai-mark logo-text-line logo-text-line--ai"'), 'index logo should render the stacked AI title line');
  assert.ok(html.includes('logo-text-line logo-text-line--primary'), 'index logo should render Generation as a dedicated title row');
  assert.ok(html.includes('logo-text-line logo-text-line--station'), 'index logo should render Station as a dedicated title row');
  assert.ok(html.includes('rel="icon"'), 'index should expose a favicon link');
  assert.ok(html.includes('id="user-panel"'), 'index should contain user panel placeholder');
  assert.ok(html.includes('class="paper-icon"'), 'index should expose the paper eye-care mode icon');
  assert.ok(authHtml.includes('class="paper-icon"'), 'auth page should expose the paper eye-care mode icon');
  assert.ok(accountHtml.includes('class="paper-icon"'), 'account page should expose the paper eye-care mode icon');
  assert.ok(adminHtml.includes('class="paper-icon"'), 'admin page should expose the paper eye-care mode icon');
  assert.ok(!html.includes('id="account-panel"'), 'index should no longer contain the account panel');
  assert.ok(!html.includes('id="admin-panel"'), 'index should no longer contain the admin panel');
  assert.ok(html.includes('id="chat-queue-indicator"'), 'index should contain chat queue indicator');
  assert.ok(html.includes('id="chat-conversation-list"'), 'index should contain chat conversation list');
  assert.ok(html.includes('id="chat-conversation-search"'), 'index should contain conversation search input');
  assert.ok(!html.includes('id="chat-filter-bar"'), 'index should no longer contain chat filter chips');
  assert.ok(!html.includes('data-chat-filter='), 'index should no longer expose conversation filter chips');
  assert.ok(!html.includes('id="chat-search-feedback"'), 'index should no longer contain search feedback panel');
  assert.ok(html.includes('id="chat-archived-section"'), 'index should contain archived conversation section');
  assert.ok(html.includes('id="chat-archived-list"'), 'index should contain archived conversation list');
  assert.ok(html.includes('data-chat-archived-toggle="true"'), 'index should contain archived conversation collapse toggle');
  assert.ok(html.includes('id="chat-conversation-title"'), 'index should contain active conversation title');
  assert.ok(!html.includes('id="workspace-resume-card"'), 'index should no longer contain the retired workspace resume card');
  assert.ok(!html.includes('id="workspace-clear-draft"'), 'index should no longer contain the retired workspace clear-draft action');
  assert.ok(html.includes('id="chat-scroll-to-latest"'), 'index should contain the chat scroll-to-latest action');
  assert.ok(html.includes('id="chat-request-status"'), 'index should contain chat request status feedback');
  assert.ok(html.includes('role="status" aria-live="polite"'), 'chat request status should announce live generation state accessibly');
  assert.ok(html.includes('id="chat-composer-meta"'), 'index should contain chat composer guidance metadata');
  assert.ok(html.includes('id="chat-draft-indicator"'), 'index should contain chat draft stage indicator');
  assert.ok(html.includes('id="chat-shortcut-hint"'), 'index should contain chat keyboard shortcut hint');
  assert.ok(html.includes('id="chat-draft-count"'), 'index should contain chat draft character count');
  assert.ok(html.includes('maxlength="4000"'), 'chat input should constrain overly long drafts');
  assert.ok(html.includes('aria-describedby="chat-draft-indicator chat-shortcut-hint chat-draft-count"'), 'chat input should reference composer guidance');
  assert.ok(html.includes('id="btn-chat-stop"'), 'index should contain chat stop-generation action');
  assert.ok(html.includes('type="button" class="sidebar-toggle"'), 'sidebar toggle should declare a non-submit button type');
  assert.ok(html.includes('type="button" class="nav-item active"'), 'active sidebar navigation button should declare a non-submit type');
  assert.ok(html.includes('type="button" class="example-chip"'), 'prompt example chips should declare a non-submit button type');
  assert.ok(html.includes('type="button" class="btn btn-primary" id="btn-generate-music"'), 'music generate action should declare a non-submit button type');
  assert.ok(html.includes('type="button" class="btn btn-secondary" id="btn-speech-stop"'), 'speech stop action should declare a non-submit button type');
  assert.ok(html.includes('id="image-modal" class="image-modal" role="dialog" aria-modal="true" aria-labelledby="image-modal-title" aria-describedby="image-modal-description"'), 'image modal should expose dialog semantics and accessible references');
  assert.ok(html.includes('id="image-modal-title" class="sr-only"'), 'image modal should expose a screen-reader-only title');
  assert.ok(html.includes('id="image-modal-description" class="sr-only"'), 'image modal should expose a screen-reader-only description');
  assert.ok(html.includes('type="button" class="close" id="modal-close" aria-label="关闭图片预览"'), 'image modal close action should declare a non-submit button type and accessible name');
  assert.ok(html.includes('id="modal-image" src="" alt="生成图片预览"'), 'image modal preview should expose a descriptive image alt');
  assert.ok(appJs.includes('const IMAGE_MODAL_FOCUSABLE_SELECTOR ='), 'image modal should define a focusable selector for focus trapping');
  assert.ok(appJs.includes('function getImageModalFocusableElements(modal)'), 'image modal should collect focusable elements for focus trapping');
  assert.ok(appJs.includes('function trapImageModalFocus(event)'), 'image modal should expose a lightweight focus trap handler');
  assert.ok(appJs.includes("event.key !== 'Tab'"), 'image modal focus trap should only handle Tab navigation');
  assert.ok(appJs.includes('event.shiftKey && document.activeElement === firstElement'), 'image modal focus trap should wrap Shift+Tab from first to last focusable element');
  assert.ok(appJs.includes('!event.shiftKey && document.activeElement === lastElement'), 'image modal focus trap should wrap Tab from last to first focusable element');
  assert.ok(workspaceInitToolsJs.includes('const trapImageModalFocus = settings.trapImageModalFocus || function () {};'), 'workspace init should accept the image modal focus trap dependency');
  assert.ok(workspaceInitToolsJs.includes('trapImageModalFocus(e);'), 'workspace init should run the image modal focus trap before other key handling');
  assert.ok(appJs.includes("$('modal-close')?.focus();"), 'image modal should move focus to the close action when opened');
  assert.ok(appJs.includes('let imageModalReturnFocusElement = null;'), 'image modal should track the element that opened the preview');
  assert.ok(appJs.includes('imageModalReturnFocusElement = document.activeElement instanceof HTMLElement ? document.activeElement : null;'), 'image modal should remember the current focus before opening');
  assert.ok(appJs.includes('document.contains(imageModalReturnFocusElement)'), 'image modal should verify the return focus target still exists');
  assert.ok(appJs.includes('imageModalReturnFocusElement.focus();'), 'image modal should restore focus after closing');
  assert.ok(appJs.includes('imageModalReturnFocusElement = null;'), 'image modal should clear the stored focus target after closing');
  assert.ok(appJs.includes("if (image) image.removeAttribute('src');"), 'image modal should clear the preview image source when closed');
  assert.ok(workspaceInitToolsJs.includes("getElement('image-modal')?.addEventListener('click'"), 'image modal should support closing by clicking the overlay');
  assert.ok(workspaceInitToolsJs.includes("if (e.target === getElement('image-modal')) closeImageModal();"), 'image modal overlay click should only close from the backdrop');
  assert.ok(workspaceInitToolsJs.includes("if (e.key === 'Escape') closeImageModal();"), 'image modal should support Escape to close');
  assert.ok(html.includes('id="lyrics-style" aria-label="选择歌词风格"'), 'lyrics style select should expose an accessible name');
  assert.ok(html.includes('id="cover-ratio" aria-label="选择封面比例"'), 'cover ratio select should expose an accessible name');
  assert.ok(html.includes('id="voice-timbre" aria-label="选择目标音色"'), 'voice timbre select should expose an accessible name');
  assert.ok(html.includes('id="speech-format" aria-label="选择语音输出格式"'), 'speech format select should expose an accessible name');
  assert.ok(html.includes('id="speech-speed" min="0.5" max="2.0" step="0.1" value="1.0" aria-label="调整语速"'), 'speech speed range should expose an accessible name');
  assert.ok(html.includes('id="speech-vol" min="0" max="100" step="5" value="50" aria-label="调整音量"'), 'speech volume range should expose an accessible name');
  [
    ['music-prompt', 'music-hint', 'music-count', 'music-prompt-error'],
    ['lyrics-prompt', 'lyrics-hint', 'lyrics-count', 'lyrics-prompt-error'],
    ['cover-prompt', 'cover-hint', 'cover-count', 'cover-prompt-error'],
    ['voice-prompt', 'voice-hint', 'voice-count', 'voice-prompt-error'],
    ['speech-text', 'speech-hint', 'speech-count', 'speech-text-error']
  ].forEach(([inputId, hintId, countId, errorId]) => {
    assert.ok(html.includes(`id="${hintId}"`), `${inputId} should expose a stable hint description`);
    assert.ok(html.includes(`id="${countId}"`), `${inputId} should expose a stable count description`);
    assert.ok(html.includes(`id="${errorId}" role="alert" hidden`), `${inputId} should expose a field-level error region`);
    assert.ok(html.includes(`id="${inputId}"`) && html.includes(`required aria-required="true" aria-describedby="${hintId} ${countId} ${errorId}"`), `${inputId} should require input and reference hint, count, and error descriptions`);
  });
  assert.ok(html.includes('id="voice-source-hint"'), 'voice source should expose a stable source hint');
  assert.ok(html.includes('id="voice-source-error" role="alert" hidden'), 'voice source should expose a field-level error region');
  assert.ok(html.includes('id="voice-drop-zone" tabindex="-1" aria-describedby="voice-source-hint voice-source-error"'), 'voice drop zone should be focusable for source validation feedback');
  assert.ok(html.includes('id="voice-audio-url"') && html.includes('aria-describedby="voice-source-hint voice-source-error"'), 'voice URL input should reference source validation feedback');
  assert.ok(workspaceGenerationToolsJs.includes('function setFieldError(inputId, message)'), 'workspace generation should provide field-level error feedback');
  assert.ok(workspaceGenerationToolsJs.includes("input.setAttribute('aria-invalid', 'true');"), 'workspace generation should mark invalid fields semantically');
  assert.ok(workspaceGenerationToolsJs.includes('input.focus?.();'), 'workspace generation should focus the first invalid field');
  assert.ok(workspaceGenerationToolsJs.includes('function requireField(inputId, message)'), 'workspace generation should centralize required field validation');
  assert.ok(workspaceGenerationToolsJs.includes('function setVoiceSourceError(message'), 'workspace generation should validate voice source separately');
  assert.ok(workspaceGenerationToolsJs.includes("requiredField: { id: 'lyrics-prompt'"), 'lyrics generation should use field-level required validation');
  assert.ok(workspacePageInitToolsJs.includes('function setSpeechFieldError(inputId, message)'), 'speech generation should expose field-level error feedback');
  assert.ok(workspacePageInitToolsJs.includes("setSpeechFieldError('speech-text', '请输入要转换的文本');"), 'speech generation should focus and mark empty speech text');
  assert.ok(workspaceInitToolsJs.includes("const error = getElement(`${id}-error`);"), 'workspace input listeners should clear field errors when users type');
  assert.ok(html.includes('id="music-generating" class="generating-card" role="status" aria-live="polite" aria-atomic="false"'), 'music generating card should expose a polite status region');
  assert.ok(html.includes('id="lyrics-generating" class="generating-card" role="status" aria-live="polite" aria-atomic="false"'), 'lyrics generating card should expose a polite status region');
  assert.ok(html.includes('id="cover-generating" class="generating-card" role="status" aria-live="polite" aria-atomic="false"'), 'cover generating card should expose a polite status region');
  assert.ok(html.includes('id="voice-generating" class="generating-card" role="status" aria-live="polite" aria-atomic="false"'), 'voice generating card should expose a polite status region');
  assert.ok(html.includes('role="progressbar" aria-labelledby="music-generating-text" aria-valuemin="0" aria-valuemax="100" aria-valuenow="0"'), 'music progress should expose progressbar semantics');
  assert.ok(html.includes('role="progressbar" aria-labelledby="lyrics-generating-text" aria-valuemin="0" aria-valuemax="100" aria-valuenow="0"'), 'lyrics progress should expose progressbar semantics');
  assert.ok(html.includes('role="progressbar" aria-labelledby="cover-generating-text" aria-valuemin="0" aria-valuemax="100" aria-valuenow="0"'), 'cover progress should expose progressbar semantics');
  assert.ok(html.includes('role="progressbar" aria-labelledby="voice-generating-text" aria-valuemin="0" aria-valuemax="100" aria-valuenow="0"'), 'voice progress should expose progressbar semantics');
  assert.ok(html.includes('id="music-progress-fill"'), 'music progress fill should remain in page markup');
  assert.ok(html.includes('id="lyrics-progress-fill"'), 'lyrics progress fill should remain in page markup');
  assert.ok(html.includes('id="cover-progress-fill"'), 'cover progress fill should remain in page markup');
  assert.ok(html.includes('id="voice-progress-fill"'), 'voice progress fill should remain in page markup');
  assert.ok(!html.includes('style="width:0%"'), 'progress fills should not use inline width styles');
  assert.ok(!/type="button"[^>]*type="button"/.test(html), 'index should not duplicate button type attributes');
  assert.ok(!html.includes('id="chat-suggestion-strip"'), 'index should no longer contain the chat suggestion strip');
  assert.ok(!html.includes('id="chat-excerpt-shelf"'), 'index should not contain the removed personal excerpt shelf');
  assert.ok(!html.includes('个人摘录'), 'index should not expose the removed personal excerpt entry copy');
  assert.ok(!html.includes('id="btn-chat-excerpt-toggle-panel"'), 'index should not contain the removed excerpt manager toggle');
  assert.ok(!html.includes('id="btn-chat-excerpt-copy-visible"'), 'index should not contain the removed copy-visible excerpt action');
  assert.ok(!html.includes('id="chat-excerpt-search"'), 'index should not contain the removed excerpt search input');
  assert.ok(!html.includes('id="chat-context-strip"'), 'index should no longer contain the chat context strip');
  assert.ok(!html.includes('id="chat-reading-outline"'), 'index should no longer contain the chat reading outline');
  assert.ok(!html.includes('id="workspace-asset-strip"'), 'index should no longer contain workspace asset strip');
  assert.ok(!html.includes('id="workspace-asset-stats"'), 'index should no longer contain workspace asset stats');
  assert.ok(html.includes('id="btn-quota-toggle"'), 'index should contain nav quota collapse toggle');
  assert.ok(html.includes('id="quota-summary"'), 'index should contain nav quota summary line');
  assert.ok(html.includes('id="btn-chat-new-conversation"'), 'index should contain new conversation button');
  assert.ok(!html.includes('id="btn-chat-rename-conversation"'), 'index should no longer contain header rename conversation button');
  assert.ok(!html.includes('id="btn-chat-archive-conversation"'), 'index should no longer contain header archive conversation button');
  assert.ok(html.includes('id="chat-model-dropdown"'), 'index should contain the chat model dropdown');
  assert.ok(html.includes('id="chat-model"'), 'index should keep the hidden default chat model input');
  assert.ok(html.includes('class="chat-input-row"'), 'index should contain the chat input row');
  assert.ok(html.includes('gpt-4.1-mini'), 'index should expose the default chat model label');
  assert.ok(html.includes('id="covervoice-result"'), 'voice result id should be aligned with app logic');
  assert.ok(html.includes('>会话<'), 'chat conversation sidebar title should be simplified and localized to Chinese');
  assert.ok(!html.includes('继续上次工作'), 'workspace should no longer surface the retired personal resume card copy');
  assert.ok(!html.includes('最近资产'), 'workspace should no longer render bottom recent-asset copy');
  assert.ok(html.includes('新建对话'), 'chat new conversation button should be localized to Chinese');
  assert.ok(html.includes('搜索会话'), 'conversation search should be localized to Chinese');
  assert.ok(html.includes('已归档'), 'archived conversation section should be localized to Chinese');
  assert.ok(html.includes('暂无进行中的对话'), 'empty active conversation title should be localized to Chinese');
  assert.ok(!html.includes('id="btn-chat-rename-conversation"'), 'chat header should no longer expose rename button copy');
  assert.ok(!html.includes('id="btn-chat-archive-conversation"'), 'chat header should no longer expose archive button copy');
  assert.ok(authHtml.includes('/js/auth-page.js'), 'auth page should load the auth page controller');
  assert.ok(authHtml.includes('name="aigs-api-base-url"'), 'auth page should expose the optional API base-url meta tag');
  assert.ok(!authHtml.includes('id="login-form"'), 'auth recovery page should not contain the login form');
  assert.ok(!authHtml.includes('id="register-form"'), 'auth recovery page should not contain the register form');
  assert.ok(authHtml.includes('id="forgot-form"'), 'auth recovery page should expose the forgot-password form');
  assert.ok(loginHtml.includes('id="login-form"'), 'login page should contain the login form');
  assert.ok(loginHtml.includes('auth-simple-page auth-simple-page--login'), 'login page should use the pure auth form layout');
  assert.ok(loginHtml.includes('auth-simple-card'), 'login page should contain a single simple auth card');
  assert.ok(!loginHtml.includes('auth-v2-brand-panel'), 'login page should not include an unrelated brand panel');
  assert.ok(!loginHtml.includes('auth-v2-preview'), 'login page should not include preview panels');
  assert.ok(!loginHtml.includes('id="register-form"'), 'login page should not contain the register form');
  assert.ok(registerHtml.includes('id="register-form"'), 'register page should contain the register form');
  assert.ok(registerHtml.includes('auth-simple-page auth-simple-page--register'), 'register page should use the pure auth form layout');
  assert.ok(registerHtml.includes('auth-simple-card'), 'register page should contain a single simple auth card');
  assert.ok(!registerHtml.includes('auth-v2-brand-panel'), 'register page should not include an unrelated brand panel');
  assert.ok(!registerHtml.includes('auth-v2-preview'), 'register page should not include preview panels');
  assert.ok(!registerHtml.includes('auth-v2-step'), 'register page should not include onboarding step panels');
  assert.ok(!registerHtml.includes('id="login-form"'), 'register page should not contain the login form');
  assert.ok(loginHtml.includes('/register/'), 'login page should link to register page');
  assert.ok(registerHtml.includes('/login/'), 'register page should link back to login page');
  assert.ok(authHtml.includes('id="token-form"'), 'auth page should expose token-based activation/reset form');
  assert.ok(accountHtml.includes('/js/account-page.js'), 'account page should load the account page controller');
  assert.ok(accountHtml.includes('name="aigs-api-base-url"'), 'account page should expose the optional API base-url meta tag');
  assert.ok(accountHtml.includes('class="account-dashboard"'), 'account page should expose the account dashboard layout');
  assert.ok(accountHtml.includes('id="account-password-form"'), 'account page should contain the account password form');
  assert.ok(accountHtml.includes('id="account-password-status-heading"'), 'account page should expose the security status summary');
  assert.ok(accountHtml.includes('id="account-admin-link"'), 'account page should expose the admin shortcut link placeholder');
  assert.ok(accountHtml.includes('id="account-overview-display-name"'), 'account page should expose a structured profile overview display-name field');
  assert.ok(accountHtml.includes('id="account-overview-plan"'), 'account page should expose a structured profile overview plan field');
  assert.ok(adminHtml.includes('/js/admin-page.js'), 'admin page should load the admin page controller');
  assert.ok(adminHtml.includes('name="aigs-api-base-url"'), 'admin page should expose the optional API base-url meta tag');
  assert.ok(adminHtml.includes('id="admin-create-user-form"'), 'admin page should contain admin create-user form');
  assert.ok(adminHtml.includes('id="admin-reset-password-form"'), 'admin page should contain admin reset-password form');
  assert.ok(adminHtml.includes('id="admin-invite-feedback"'), 'admin page should contain admin invitation feedback area');
  assert.ok(adminHtml.includes('id="admin-audit-form"'), 'admin page should contain admin audit filter form');
  assert.ok(adminHtml.includes('value="user_public_register"'), 'admin audit filters should expose the public-registration action');
  assert.ok(conversationActionToolsJs.includes('会话已归档'), 'conversation archive success toast should be localized to Chinese');
  assert.ok(conversationActionToolsJs.includes('确认归档“'), 'conversation archive confirmation should be localized to Chinese');
  assert.ok(conversationActionToolsJs.includes('请等待当前回复完成后再切换会话。'), 'conversation switch guard should be localized to Chinese');
  assert.ok(conversationActionToolsJs.includes('已归档会话已删除'), 'archived conversation delete success toast should be localized to Chinese');
  assert.ok(html.includes('class="message-body"'), 'chat welcome message should use structured message-body markup');
  assert.ok(chatMessageNodeToolsJs.includes('正在思考'), 'workspace should surface a visible thinking state before chat replies stream in');
  assert.ok(appJs.includes('formatChatMessageHtml'), 'workspace should format chat replies with structured rich text rendering');
  assert.ok(appJs.includes('updateDropdownScrollState'), 'workspace should keep the chat model dropdown scroll hint state in sync');
  assert.ok(workspaceChatModelToolsJs.includes('dropdown-scroll-hint'), 'workspace chat-model utilities should render a scroll hint for long chat model lists');
  assert.ok(workspaceUiToolsJs.includes("menu.addEventListener('click'"), 'workspace ui utilities should delegate chat model option clicks after async model loading');
  assert.ok(appJs.includes('initializeChatModelDropdownLoadingState'), 'workspace should initialize chat model dropdown loading state before async models return');
  assert.ok(workspaceChatModelToolsJs.includes('正在加载模型列表...'), 'workspace chat-model utilities should expose a visible loading placeholder for chat model dropdown initialization');
  assert.ok(appJs.includes('requireWorkspaceChatModelTools'), 'workspace should wire the dedicated chat-model module through app assembly');
  assert.ok(workspaceChatModelToolsJs.includes('loadChatModelOptions'), 'workspace chat-model utilities should own chat model remote loading');
  assert.ok(workspaceInitToolsJs.includes('loadChatModelOptions'), 'workspace init utilities should start loading chat models without blocking the rest of chat initialization');
  assert.ok(chatStreamToolsJs.includes('conversation_state'), 'workspace should handle streamed conversation state events');
  assert.ok(appJs.includes('rewriteAssistantMessage'), 'workspace should expose assistant rewrite actions');
  assert.ok(appJs.includes('copyAssistantMessage'), 'workspace should expose assistant copy actions');
  assert.ok(chatMessageActionToolsJs.includes('rewriteAssistantMessage'), 'chat action utilities should own assistant rewrite actions');
  assert.ok(chatMessageActionToolsJs.includes('switchAssistantVersion'), 'chat action utilities should own assistant version switching');
  assert.ok(!appJs.includes('toggleChatExcerpt'), 'workspace should not expose removed chat excerpt toggle actions');
  assert.ok(!appJs.includes('renderChatExcerptShelf'), 'workspace should not render the removed chat excerpt shelf');
  assert.ok(!appJs.includes('jumpToChatExcerpt'), 'workspace should not support removed excerpt jump actions');
  assert.ok(!appJs.includes('copyChatExcerptBundle'), 'workspace should not support removed excerpt bundle copying');
  assert.ok(!appJs.includes('insertChatExcerptIntoComposer'), 'workspace should not support reusing removed excerpt content in composer');
  assert.ok(!appJs.includes('renderWorkspaceAssetStrip'), 'workspace should not keep a retired bottom asset-strip helper');
  assert.ok(!appJs.includes('applyChatAssetToCurrentWorkspace'), 'workspace should not keep dead bottom asset application logic');
  assert.ok(appJs.includes('buildAssistantMessageCompactSummary'), 'workspace should build compact summaries for long assistant replies');
  assert.ok(appJs.includes('toggleAssistantMessageCompact'), 'workspace should toggle long assistant reply compact mode');
  assert.ok(!appJs.includes('setChatExcerptArchived'), 'workspace should not support removed excerpt archive actions');
  assert.ok(!appJs.includes('clearArchivedChatExcerpts'), 'workspace should not support clearing removed archived excerpts');
  assert.ok(!appJs.includes('openArchivedChatAssetsPanel'), 'workspace should not keep dead bottom asset panel helpers');
  assert.ok(appJs.includes('retryTransientAssistantMessage'), 'workspace should expose retry actions for failed assistant replies');
  assert.ok(chatMessageNodeToolsJs.includes('data-chat-message-actions-toggle'), 'workspace should expose a compact assistant action toggle for version history');
  assert.ok(appJs.includes('stopChatGeneration'), 'workspace should expose stop-generation behavior');
  assert.ok(appJs.includes('groupConversationsByDay'), 'workspace should group conversation list items by relative day');
  assert.ok(appJs.includes('toggleConversationWorkflowState'), 'workspace should support local conversation workflow toggles');
  assert.ok(appJs.includes('isConversationPinned'), 'workspace should expose pinned conversation workflow state');
  assert.ok(appJs.includes('isConversationParked'), 'workspace should expose parked conversation workflow state');
  assert.ok(!chatComposerToolsJs.includes('renderChatSuggestionStrip'), 'chat composer utilities should not keep the retired adaptive suggestion strip renderer');
  assert.ok(chatComposerToolsJs.includes('getChatExperienceStage'), 'chat composer utilities should adapt chat composer copy to the current conversation stage');
  assert.ok(appJs.includes('syncChatViewportState'), 'workspace should react to mobile visual viewport changes while composing');
  assert.ok(appJs.includes('requireWorkspaceGenerationTools'), 'workspace should wire the dedicated generation module through app assembly');
  assert.ok(appJs.includes('requireWorkspaceMediaTools'), 'workspace should wire the dedicated media module through app assembly');
  assert.ok(appJs.includes('requireWorkspaceConversationTools'), 'workspace should wire the dedicated conversation module through app assembly');
  assert.ok(appJs.includes('requireWorkspaceStateTools'), 'workspace should wire the dedicated state module through app assembly');
  assert.ok(appJs.includes('requireWorkspaceShellTools'), 'workspace should wire the dedicated shell module through app assembly');
  assert.ok(appJs.includes('requireWorkspaceUiTools'), 'workspace should wire the dedicated ui module through app assembly');
  assert.ok(appJs.includes('requireWorkspacePageInitTools'), 'workspace should wire the dedicated page-init module through app assembly');
  assert.ok(appJs.includes('requireWorkspaceAuthTools'), 'workspace should wire the dedicated auth module through app assembly');
  assert.ok(appJs.includes('requireWorkspaceInitTools'), 'workspace should wire the dedicated init module through app assembly');
  assert.ok(appJs.includes('requireChatEntryTools'), 'workspace should wire the dedicated chat entry module through app assembly');
  assert.ok(appJs.includes('requireChatMessageMetaTools'), 'workspace should wire the dedicated chat message meta module through app assembly');
  assert.ok(appJs.includes('requireChatMessageActionTools'), 'workspace should wire the dedicated chat action module through app assembly');
  assert.ok(appJs.includes('requireChatComposerTools'), 'workspace should wire the dedicated chat composer module through app assembly');
  assert.ok(workspaceGenerationToolsJs.includes('/api/generate/music'), 'workspace generation utilities should own music generation orchestration');
  assert.ok(workspaceGenerationToolsJs.includes('/api/generate/cover'), 'workspace generation utilities should own cover generation orchestration');
  assert.ok(workspaceGenerationToolsJs.includes('/api/generate/voice'), 'workspace generation utilities should own voice generation orchestration');
  assert.ok(workspaceConversationToolsJs.includes('applyConversationPayload'), 'workspace conversation utilities should own conversation payload orchestration');
  assert.ok(workspaceConversationToolsJs.includes('renderConversationMeta'), 'workspace conversation utilities should own conversation header rendering');
  assert.ok(workspaceConversationToolsJs.includes('createConversationAndSelect'), 'workspace conversation utilities should own conversation creation orchestration');
  assert.ok(workspaceStateToolsJs.includes('persistWorkspaceState'), 'workspace state utilities should own workspace draft persistence');
  assert.ok(workspaceStateToolsJs.includes('clearFeatureDraft'), 'workspace state utilities should own workspace draft cleanup');
  assert.ok(workspaceInitToolsJs.includes('bindWorkspaceInteractions'), 'workspace init utilities should own workspace interaction binding');
  assert.ok(workspaceInitToolsJs.includes('initMobileSidebar'), 'workspace init utilities should own mobile sidebar binding');
  assert.ok(appJs.includes('setChatArchivedCollapsed'), 'workspace should support collapsing the archived conversation section');
  assert.ok(appJs.includes('syncQuotaCardState'), 'workspace should keep nav quota collapsed state in sync');
  assert.ok(workspaceShellToolsJs.includes("['dark', 'light', 'paper']"), 'workspace theme toggle should support paper eye-care mode');
  assert.ok(workspaceShellToolsJs.includes("paper: '护眼模式'"), 'workspace theme toggle should label paper eye-care mode');
  assert.ok(workspaceShellToolsJs.includes('normalizeTheme'), 'workspace theme toggle should normalize unknown theme values');
  assert.ok(siteShellJs.includes("['dark', 'light', 'paper']"), 'portal theme toggle should support paper eye-care mode');
  assert.ok(siteShellJs.includes('showWelcomeToast'), 'portal shell should expose a welcome toast helper for auth success feedback');
  assert.ok(siteShellJs.includes('queueWelcomeToast'), 'portal shell should queue auth success feedback before redirect');
  assert.ok(siteShellJs.includes('consumeQueuedWelcomeToast'), 'portal shell should consume auth success feedback after landing');
  assert.ok(authHtml.includes('id="auth-entry-head"'), 'auth recovery page should expose a standard entry heading block');
  assert.ok(authHtml.includes('账号恢复与邀请激活'), 'auth recovery page should present recovery-focused copy');
  assert.ok(loginHtml.includes('登录'), 'login page should present login-focused copy');
  assert.ok(registerHtml.includes('注册账号'), 'register page should present register-focused copy');
  assert.ok(authPageJs.includes('queueWelcomeToast'), 'auth page should queue the welcome toast before redirect');
  assert.ok(authPageJs.includes('redirectTokenIntentToRecoveryPage'), 'auth page should hand token links back to recovery page');
  assert.ok(!authPageJs.includes('await SiteShell.showWelcomeToast'), 'auth page should not wait for welcome toast before entering workspace');
  assert.ok(authPageJs.includes('账号已创建，工作台已准备好'), 'auth page should contain a register success flow');
  assert.ok(authPageJs.includes('账号已激活'), 'auth page should contain an invite-activation success flow');
  assert.ok(accountPageJs.includes('密码已更新，当前会话已保留。'), 'account page should surface password update success feedback');
  assert.ok(adminPageJs.includes('已撤销'), 'admin page should support invitation revocation copy');
  assert.ok(adminHtml.includes('审计日志'), 'admin page should expose localized audit log copy');
  assert.ok(workspaceAuthToolsJs.includes('登录状态已失效，请重新登录'), 'session expiry copy should be localized to Chinese');
  assert.ok(workspaceAuthToolsJs.includes('/login/'), 'workspace should redirect unauthenticated users to the login page');
  assert.ok(workspaceAuthToolsJs.includes('/account/'), 'workspace should link authenticated users to the account page');
  assert.ok(workspaceAuthToolsJs.includes('/admin/'), 'workspace should link admin users to the admin page');

  console.log('✅ Page markup tests passed');
}

if (require.main === module) {
  main();
}

module.exports = {
  main
};
