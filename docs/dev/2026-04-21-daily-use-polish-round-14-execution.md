# 2026-04-21 Daily Use Polish Round 14 Execution

## 本轮范围
- 计划改动文件：`public/js/app.js`
- 文档文件：`docs/dev/2026-04-21-daily-use-polish-round-14-plan.md`、`docs/dev/2026-04-21-daily-use-polish-round-14-execution.md`、`docs/dev/2026-04-21-daily-use-polish-round-14-regression.md`
- 约束：不触碰外部脏文件 `public/js/app-shell.js`

## 执行记录
### TODO 1 复核现状
- 已复核 round-13 会话工作流实现，确认最主要的残余问题有两处：同一会话可以同时处于 `重点` 和 `稍后处理` 两种状态，以及会话归档后仍可能保留活跃列表工作流标记。

#### 单任务复盘
- 新问题：这类“语义冲突”不会在第一眼就报错，但会慢慢污染排序、筛选和标签解释。
- 边界条件：修复必须保持本地工作流方案，不引入新的远端字段或迁移步骤。
- 遗漏补齐：归档属于生命周期切换，不能只盯着列表排序，还要同步清理工作流状态。

### TODO 2 修复工作流互斥逻辑
- 调整了本地工作流状态归一化逻辑，确保 `parkedIds` 会自动排除掉已经在 `pinnedIds` 中的会话。
- 调整了 `toggleConversationWorkflowState`，当某个会话被设为 `重点` 时会自动移出 `稍后`；反过来设为 `稍后` 时也会自动移出 `重点`。

#### 单任务复盘
- 新问题：如果一个会话同时是 `重点` 又是 `稍后`，排序和标签都会自相矛盾，用户无法理解系统到底想表达什么。
- 边界条件：互斥逻辑必须同时覆盖“读取旧状态”和“写入新状态”两个阶段，不能只修其中一个。
- 遗漏补齐：历史上已经写入本地的冲突状态也通过归一化逻辑自动修正，避免用户手动清理。

### TODO 3 修复归档状态清理
- 在归档会话时同步调用工作流状态清理，确保活跃列表的 `重点 / 稍后处理` 语义不会继续泄漏到归档生命周期里。
- 保留了删除已归档会话时的工作流清理逻辑，与归档行为形成一致规则。

#### 单任务复盘
- 新问题：如果归档后仍带着活跃工作流标记，筛选和摘要就会出现“这个会话到底算活跃还是已处理”的歧义。
- 边界条件：清理逻辑应只在生命周期切换到归档时发生，不影响普通的会话内容更新与排序。
- 遗漏补齐：删除归档和归档本身现在都走统一的“移出工作流状态”规则，避免两条路径表现不一致。

### TODO 4 文档收尾
- 已补充 round-14 计划、执行、回归文档，记录这次修复是针对 round-13 工作流语义一致性的后续收口。

#### 单任务复盘
- 新问题：这种“续修复”轮次如果不单独留档，后续只看 round-13 会误以为工作流语义从一开始就是完整的。
- 边界条件：文档需要突出这轮是小范围逻辑收紧，而不是新的功能扩散。
- 遗漏补齐：测试结果与提交范围会继续单独记录在回归文档中。

### TODO 5 回归与收尾
- 已完成语法检查、页面结构断言与前端烟测。
- 已确认本轮提交仅包含 `public/js/app.js` 与 round-14 文档，继续排除 `public/js/app-shell.js`。

#### 单任务复盘
- 新问题：这类逻辑修复的风险主要在“不会立刻崩，但会在真实使用里逐步累积混乱”，所以功能烟测比视觉改动更关键。
- 边界条件：本轮没有新增视觉结构，不再重复做视觉基线刷新。
- 遗漏补齐：保留了原有静态断言与烟测链路，确保修复没有打断 round-13 的交互主链。

## 测试与结果
- 详见 `docs/dev/2026-04-21-daily-use-polish-round-14-regression.md`
