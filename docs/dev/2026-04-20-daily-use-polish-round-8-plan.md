# 2026-04-20 Daily Use Polish Round 8 Plan

## 目标
- 继续推进“长期天天用”的聊天页打磨，聚焦更强的会话筛选、目录阅读跟随和最近模板管理。
- 本轮继续只做前端增强，不触碰后端接口与现有持久化协议之外的结构。

## 设计方向
- 会话筛选：除了搜索词，还要有低摩擦的快捷范围切换，让用户快速只看当前、空白或今日活跃会话。
- 目录阅读：长回复目录不只是可点击，还要能随着滚动告诉用户“现在正在看哪一节”。
- 最近模板：最近使用不该只会累积，也要有可清理能力，避免越用越乱。

## 顺序化 TODO
1. 审查会话筛选、目录跟随与最近模板管理边界，补齐 round-8 文档。
2. 优化会话侧栏：增加快捷筛选维度与当前筛选状态提示。
3. 优化长回复目录：支持当前段落高亮和阅读过程中的自动跟随。
4. 优化最近模板管理：增加清空能力并处理无效引用后的展示收敛。
5. 更新执行日志，执行前端回归测试，确认通过后提交并推送。

## 验收标准
- 用户能一键筛到当前会话、空白会话或今日活跃会话，并清楚看到当前筛选状态。
- 长回复目录会跟随当前阅读位置高亮，而不是只负责跳转。
- 最近模板区域可以一键清空，失效引用不会继续占位。
