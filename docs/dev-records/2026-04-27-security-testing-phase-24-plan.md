# 2026-04-27 安全性测试第二十四阶段计划

## 目标
- 完成 `ui-ux-pro-max-0.1.0\cli` 子工程的一次真实最小运行时 smoke，验证构建产物不仅能构建，也能实际启动并执行只读命令。
- 若命中问题，做最小修复并复测；否则形成最终运行时结论并回写文档。

## 范围
- 插件子工程：
  - `ui-ux-pro-max-0.1.0\cli`
- 重点产物：
  - `dist\index.js`
- 文档记录：
  - `docs\dev-records`
  - `docs\test\security-testing-report.md`

## 非范围
- 不执行会修改当前主仓库内容的 CLI 安装命令。
- 不继续扩展主仓库安全测试矩阵。
- 不做插件目录全面功能测试。

## 假设
- `versions` 命令属于只读命令，适合做最小 smoke。
- 若只读命令能正常运行，说明 CLI 的核心入口、命令路由和依赖解析已打通。

## 风险
- 若直接运行 `init` 之类命令，可能写入当前目录，扩大改动面。
- 运行时 smoke 需要尽量只选只读命令，避免把环境写脏。

## 完成标准
- 已完成一次只读 CLI smoke。
- 若命中问题，完成最小修复并复测。
- 本轮目标、TODO、执行记录、验证结果、复盘全部写入 `docs\dev-records`。

## 验证方式
- `node dist\index.js versions`
- 必要时 `node dist\index.js --help`

## TODO
1. 盘点适合的只读 smoke 命令。
2. 执行 CLI 最小 smoke。
3. 若失败，判断是构建产物问题、运行时依赖问题还是环境问题。
4. 若是代码问题，做最小修复并复测。
5. 回写执行记录、验证结果、复盘。
6. 若有代码改动，提交 Git。

## 执行顺序
1. 选择只读命令。
2. 执行 smoke。
3. 问题归因。
4. 必要修复与复测。
5. 文档回写。
6. Git 提交。

## 执行记录
- 已完成 TODO 1：
  - 已选择不会修改当前工作区的只读命令作为 smoke：
    - `node dist\index.js --help`
    - `node dist\index.js versions`
- 已完成 TODO 2：
  - 已执行 CLI 最小 smoke。
  - 当前结果：
    - 帮助命令正常输出
    - `versions` 命令正常拉取并输出版本列表
- 已完成 TODO 3：
  - 本轮未命中运行时问题。
  - 当前可以确认插件 CLI 在以下层面都已打通：
    - 依赖安装
    - 构建产物生成
    - 只读运行时命令执行
- 已完成 TODO 4：
  - 本轮无需新增代码修复。
- 已完成 TODO 5：
  - 已回写执行记录、验证结果与复盘。
- TODO 6 待执行：
  - 待本轮 Git 提交。

## 验证结果
- CLI 帮助命令通过：
  - `node dist\index.js --help`
- CLI 只读版本查询命令通过：
  - `node dist\index.js versions`

## 复盘
- 新问题：
  - 本轮没有打出新的运行时问题。
  - 当前插件 CLI 已完成依赖安装、构建、最小 smoke 三层闭环。
- 边界条件：
  - 本轮只验证了只读命令，没有执行会写工作区的 `init`、`update`、`uninstall`。
- 遗漏点：
  - 若要继续深入，还可以补 `init --offline` 的隔离目录 smoke。
- 残余风险：
  - 当前残余风险主要在“写入型命令的运行时行为”，而不是构建或只读命令链路。
- 是否回写规划：
  - 是。本轮已回写运行时 smoke 结果。

## 当前结果
- 第二十四阶段已完成：
  - 插件 CLI 最小运行时 smoke 通过
  - 当前插件子工程已完成依赖、构建、只读运行三层验证

## 下一阶段建议 TODO
1. 若继续插件治理，可补隔离目录下的 `init --offline` smoke。
2. 若回到主仓库安全主线，可继续更系统化的 `Host / Origin` 组合矩阵。
3. 若准备彻底收口，可统一提交本轮运行时验证与报告更新。
