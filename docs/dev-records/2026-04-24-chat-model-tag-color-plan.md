# 聊天模型标签颜色分层计划

## 目标
- 为聊天模型标签补充颜色分层。
- 让常见标签更直观可识别，例如：
  - `推荐`：青色
  - `高质量`：金色
  - `快速`：绿色
  - `预览`：紫色

## 范围
- 仅处理聊天模型标签的展示样式与必要渲染标记。
- 修改范围限定在：
  - `public\js\app.js`
  - `public\css\style.css`
- 不改后端请求逻辑，不改模型真实值。

## 假设
- 当前模型标签数据已存在，只需补充前端展示层次。
- 本轮只做颜色分层，不改变推荐模型和分组逻辑。

## TODO
1. 给标签渲染补充语义 class。
2. 为不同标签定义颜色层级。
3. 执行最小前端回归并回写结果。

## 完成标准
- 常见标签颜色分层清晰。
- 下拉框仍保持可读、不过度花哨。
- 前端相关测试继续通过。

## 验证方式
- 执行：
  - `node test-page-markup.js`
  - `node test-frontend-state.js`

## 执行记录
- 2026-04-24 已建立本计划，待执行。

## 验证结果
- 待执行。

## 复盘
- 待执行。
