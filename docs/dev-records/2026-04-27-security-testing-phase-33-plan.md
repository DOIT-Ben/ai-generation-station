# 2026-04-27 安全性测试第三十三阶段计划

## 目标
- 评估是否要把非 API 路径的缓存头从“现状锁定”升级为“正式策略设计”。
- 若收益明确且改动面可控，则做最小收口；否则形成“不改动”的正式结论。

## 范围
- 后端实现：
  - `server\index.js`
  - 必要时 `server\lib\http.js`
- 测试脚本：
  - `test-security-gateway.js`
- 文档记录：
  - `docs\dev-records`
  - `docs\test\security-testing-report.md`

## 非范围
- 不继续扩展 `Host / Origin` 组合矩阵。
- 不修改插件 CLI。
- 不做 UI、数据库结构、认证逻辑改动。

## 假设
- 当前 API 已统一 `no-store`，非 API 路径仍是“未显式设计”状态。
- 是否继续设计化，应取决于是否存在明确安全收益和是否会扩大改动面。

## 风险
- 若直接给所有非 API 路径加统一缓存头，可能误伤静态资源合理缓存。
- 若对 HTML、CSS、404 一次性全改，容易把当前简单策略变复杂。

## 完成标准
- 已明确是否要对非 API 路径做缓存策略设计化。
- 若实施，完成最小改动与回归。
- 若不实施，完成正式结论归档。
- 本轮目标、TODO、执行记录、验证结果、复盘全部写入 `docs\dev-records`。

## 验证方式
- 现状盘点
- 必要时 `node --check server\\index.js`
- 必要时 `node --check test-security-gateway.js`
- 必要时 `node test-security-gateway.js`
- 必要时 `node test-auth-history.js`

## TODO
1. 盘点非 API HTML、静态资源、404 的缓存头现状。
2. 评估当前行为与安全收益的差距。
3. 判断是否值得做最小设计化改动。
4. 若值得，实施最小改动并更新回归。
5. 若不值得，形成正式不改动结论。
6. 回写执行记录、验证结果、复盘。
7. 若有代码改动，提交 Git。

## 执行顺序
1. 现状盘点。
2. 收益评估。
3. 改动决策。
4. 必要实现与回归。
5. 文档回写。
6. Git 提交。

## 执行记录
- 已完成 TODO 1：
  - 已盘点非 API HTML、静态资源、404 的缓存头现状：
    - 当前不显式设置 `Cache-Control`
    - 当前不显式设置 `Pragma`
    - 当前不显式设置 `Expires`
- 已完成 TODO 2：
  - 已评估当前行为与安全收益差距：
    - 现有高风险 API 已统一 `no-store`
    - 非 API 路径当前没有证据表明存在必须立即收口的安全缺陷
    - 若继续设计化非 API 路径缓存策略，改动面会明显扩大，并且容易误伤静态资源的合理缓存空间
- 已完成 TODO 3：
  - 已做出本轮决策：
    - 不实施非 API 路径缓存策略改动
    - 形成正式“不改动”结论归档
- 已完成 TODO 4：
  - 本轮未实施代码改动，因此无需新增回归。
- 已完成 TODO 5：
  - 已回写执行记录、验证结果与复盘。
- TODO 7 待执行：
  - 待本轮 Git 提交。

## 验证结果
- 已完成非 API 路径缓存头现状盘点。
- 已完成收益与改动面评估。
- 当前结论：
  - 本轮不建议继续设计化非 API 路径缓存策略。

## 复盘
- 新问题：
  - 本轮没有打出新的缓存相关实现问题。
  - 当前最小安全收益最大的缓存收口已经在 API 层完成。
- 边界条件：
  - 本轮不改非 API 缓存策略，不代表这些路径“最佳”，而是当前不值得在本轮扩大改动面。
- 遗漏点：
  - 若未来产品层面明确要求 HTML 不缓存或静态资源强缓存，再单独立题更合适。
- 残余风险：
  - 当前残余风险主要是“非 API 路径缓存策略尚未设计化”，而不是已确认的安全漏洞。
- 是否回写规划：
  - 是。本轮已回写不改动结论。

## 当前结果
- 第三十三阶段已完成：
  - 已评估非 API 路径缓存策略设计化收益
  - 已形成正式不改动结论

## 下一阶段建议 TODO
1. 若继续主线，可冻结当前口径并结束本轮。
2. 若未来业务要求变化，再单独立题设计非 API 缓存策略。
