# 聊天模型下拉仅显示一个模型排障计划

## 目标
- 查清聊天模型下拉仍然只显示一个模型的真实原因。
- 在不靠猜测的前提下修复问题，并验证前端确实能展示完整模型列表。

## 范围
- 仅处理聊天模型下拉相关前后端与运行态问题。
- 检查范围包括：
  - 当前在线实例 `18791`
  - `server\routes\service.js`
  - `public\js\app.js`
  - 本地运行进程状态

## 假设
- 可能原因包括：
  1. 当前在线实例是旧进程，未加载新的 `/api/chat/models` 逻辑
  2. 前端初始化时序或下拉绑定有问题
  3. 动态模型列表已返回，但未正确渲染

## 风险
- 若直接在未确认原因前改代码，容易把“运行态问题”误判为“实现问题”。
- 若需要重启在线实例，需先确认不会误伤当前可用服务。

## TODO
1. 检查当前在线实例 `/api/chat/models` 的真实响应。
2. 检查当前前端加载的 `app.js` 是否包含最新模型加载逻辑。
3. 检查 `initCustomDropdown` 与 `loadChatModelOptions` 的时序和事件绑定。
4. 若确认是旧进程问题，则重启本地服务到最新代码。
5. 验证模型下拉显示是否恢复正常，并回写记录。

## 完成标准
- 明确定位根因。
- 若需要修复，则完成修复。
- 前端模型下拉能显示完整模型列表。

## 验证方式
- 读取代码。
- 请求在线接口。
- 必要时重启服务并再次验证。

## 执行记录
- 2026-04-24 已建立本计划，待执行。

## 验证结果
- 待执行。

## 复盘
- 待执行。
