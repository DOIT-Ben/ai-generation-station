# 2026-04-27 安全测试矩阵冻结记录

## 目标
- 冻结当前安全测试矩阵口径，结束本轮主线，不再继续扩展边缘样本。

## 冻结范围
- 主仓库 `Host / Origin` 组合矩阵
- `ALLOWED_ORIGINS` 规范化组合矩阵
- API / 非 API 头部与缓存相关回归
- 插件 CLI 第一轮治理与多平台抽样 smoke

## 冻结原因
- 当前矩阵已经覆盖到：
  - punycode / Unicode
  - 默认端口
  - IPv6 显式端口
  - IPv6 默认端口
  - 裸 IPv6
  - `ALLOWED_ORIGINS` 规范化链路
- 最近几轮新增样本的信息增益已明显下降。
- 当前没有新的主线阻塞问题。

## 冻结后结论
- 本轮主仓库安全主线可以视为完成。
- 本轮插件 CLI 治理第一轮可以视为完成。
- 后续如果继续，应视为新一轮扩覆盖，而不是继续补本轮主线缺口。

## 后续建议
1. 若未来继续主线，优先扩更大规模 `Host / Origin` 组合矩阵。
2. 若未来业务需求变化，再单独立题设计非 API 缓存策略。
3. 若未来继续插件治理，再扩大未抽样平台覆盖。
