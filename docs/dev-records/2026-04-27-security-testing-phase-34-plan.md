# 2026-04-27 安全性测试第三十四阶段计划

## 目标
- 继续扩展更大规模的 `Host / Origin` 组合矩阵。
- 本轮优先覆盖：
  - 默认端口与应用端口的更多交叉组合
  - IPv6 与默认端口的更多组合
  - Unicode / punycode 与允许来源列表的更多交叉组合

## 范围
- 测试脚本：
  - `test-security-gateway.js`
- 后端实现核验：
  - `server\lib\request-security.js`
  - `server\index.js`
- 文档记录：
  - `docs\dev-records`
  - `docs\test\security-testing-report.md`

## 非范围
- 不继续插件 CLI 治理。
- 不修改缓存策略。
- 不做 UI、数据库结构、认证业务逻辑改动。

## 假设
- 当前矩阵覆盖已经较高，但仍可以通过少量样本进一步压实默认端口、IPv6 和允许来源列表的交叉行为。

## 风险
- 若样本设计不清晰，容易与已有样本重复，浪费测试复杂度。
- 仍然要避免一次性铺太大矩阵。

## 完成标准
- 已补一组新的高价值 `Host / Origin` 交叉样本。
- 若命中问题，完成最小修复并通过回归。
- 本轮目标、TODO、执行记录、验证结果、复盘全部写入 `docs\dev-records`。

## 验证方式
- `node --check test-security-gateway.js`
- `node test-security-gateway.js`
- `node test-auth-history.js`

## TODO
1. 盘点下一组高价值 `Host / Origin` 交叉样本。
2. 选定应允许的组合。
3. 选定应拒绝的组合。
4. 补自动化回归。
5. 运行安全与认证回归。
6. 若命中问题，做最小修复并复测。
7. 回写执行记录、验证结果、复盘。
8. 若有代码改动，提交 Git。

## 执行顺序
1. 组合盘点。
2. 允许样本回归。
3. 拒绝样本回归。
4. 执行与修复。
5. 文档回写。
6. Git 提交。

## 执行记录
- 已完成 TODO 1：
  - 已盘点下一组高价值交叉样本：
    - 允许来源列表中的 IPv6 精确匹配
    - 允许来源列表中的默认端口规范化
    - 允许来源列表中的 IPv6 端口不一致
    - 允许来源列表中的 punycode 端口不一致
- 已完成 TODO 2：
  - 已在 `testSameOriginAndAllowedCors()` 中补充应允许的组合：
    - `Origin: http://[::1]:18860`
    - 默认端口显式等价组合继续压实
- 已完成 TODO 3：
  - 已补充应拒绝的组合：
    - `Origin: http://[::1]:18861`
    - `Origin: http://xn--fsqu00a:18807`
- 已完成 TODO 4：
  - 已执行主仓库安全与认证回归。
  - 本轮新增用例全部通过，没有打出新的允许来源组合缺陷。
- 已完成 TODO 5：
  - 因本轮未发现新的真实问题，无需新增代码修复。
- 已完成 TODO 6：
  - 已回写执行记录、验证结果与复盘。
- TODO 8 待执行：
  - 待本轮 Git 提交。

## 验证结果
- 语法检查通过：
  - `node --check test-security-gateway.js`
- 安全测试通过：
  - `node test-security-gateway.js`
- 认证回归通过：
  - `node test-auth-history.js`

## 复盘
- 新问题：
  - 本轮没有打出新的实现问题。
  - 本轮把允许来源列表中的 IPv6 与端口差异样本也纳入了正式矩阵。
- 边界条件：
  - 当前仍在做高价值样本扩展，而不是全量笛卡尔组合。
- 遗漏点：
  - 更大规模矩阵仍可继续扩，但收益继续下降。
- 残余风险：
  - 当前未发现允许来源列表在 IPv6 与端口差异层存在新的错误放行问题。
- 是否回写规划：
  - 是。本轮已回写新增矩阵覆盖与结论。

## 当前结果
- 第三十四阶段已完成：
  - 新增允许来源列表中的 IPv6 与端口差异矩阵回归
  - 当前未发现新的允许来源组合缺陷

## 下一阶段建议 TODO
1. 若继续主线，可停止扩矩阵，改为冻结当前口径。
2. 若仍要继续，只建议按需补极少量高价值样本。
