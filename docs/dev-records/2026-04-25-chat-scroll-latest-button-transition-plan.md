# 2026-04-25 Chat Scroll Latest Button Transition Plan

## 目标
- 让“回到最新回复”按钮的出现与消失更顺滑，减少突兀感。

## 范围
- `public\index.html`
- `public\js\app.js`
- `public\css\style.css`
- 与按钮显示状态直接相关的最小测试

## 假设
- 当前按钮结构、位置、图标和双状态都已稳定，本轮仅细化显示过渡体验。
- 不改变按钮出现条件，只改变可见状态切换的呈现方式。

## 风险
- 若把隐藏逻辑完全改成样式控制，可能影响首屏未初始化时的闪烁行为。
- 若过渡时间过长，会让按钮显得拖沓。

## TODO
1. 将按钮显示状态拆分为“已挂载但隐藏”和“可见”两层。
2. 为按钮补充透明度、位移和可点击状态过渡。
3. 更新最小测试并执行验证。
4. 回写执行记录与复盘。

## 完成标准
- 按钮显示和隐藏都更平滑。
- 首屏不闪，按钮不可见时也不会误点击。
- 相关测试与回归通过。

## 验证方式
- 最小结构测试。
- 无 live 主回归。
- 浏览器视觉回归。

## 执行记录
- 2026-04-25：已确认本轮只细化按钮显示过渡，不扩展到其它聊天元素动画。
- 2026-04-25：按钮结构保留 `hidden` 作为首屏防闪保护，同时新增 `data-visible` 区分“已挂载但隐藏”和“可见”两层状态。
- 2026-04-25：`public\css\style.css` 为 `.chat-scroll-to-latest` 增加透明度、位移、可点击状态与 hover 过渡，避免硬切换。
- 2026-04-25：`test-chat-scroll-latest-button.js` 已补充对 `data-visible`、透明度和点击穿透状态的断言。
- 2026-04-25：执行 `node test-chat-scroll-latest-button.js` 通过。
- 2026-04-25：执行 `node test-ui-visual.js --port 18812 --launch-server` 通过；浏览器启动因沙箱限制需要提权执行。
- 2026-04-25：执行 `node test-regression.js --skip-live --port 18814` 通过，确认按钮过渡改动未破坏聚合回归。

## 复盘
- 本轮把“首屏不闪”和“切换顺滑”拆成结构层与样式层处理，改动面保持在按钮本身，没有牵连聊天主滚动逻辑。
- 浏览器类验证在当前环境下仍受 `spawn EPERM` 沙箱限制，后续凡涉及 Playwright 验证，应默认纳入提权测试步骤说明。
