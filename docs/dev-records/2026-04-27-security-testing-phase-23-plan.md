# 2026-04-27 安全性测试第二十三阶段计划

## 目标
- 继续按治理收口顺序推进，在 `ui-ux-pro-max-0.1.0\cli` 子工程中安装依赖并完成一次真实构建验证。
- 若构建失败，区分环境问题与代码问题；若是代码问题，做最小修复并复测。

## 范围
- 插件子工程：
  - `ui-ux-pro-max-0.1.0\cli`
- 重点文件：
  - `package.json`
  - `package-lock.json`
  - `src\utils\extract.ts`
- 文档记录：
  - `docs\dev-records`
  - `docs\test\security-testing-report.md`

## 非范围
- 不继续扩展主仓库安全测试矩阵。
- 不修改主仓库业务逻辑。
- 不做插件目录全面功能开发。

## 假设
- 上一轮构建失败主要是因为依赖未安装，而不是 `extract.ts` 的改动导致。
- 安装依赖后，CLI 子工程应可完成基础构建，或至少给出更准确的失败原因。

## 风险
- 安装依赖会修改该子工程的工作区状态，必须只收口必要变更。
- 若构建拉出新的类型或运行时问题，需要严格控制修复范围，避免演变成子工程全面维护。

## 完成标准
- 已安装 `ui-ux-pro-max-0.1.0\cli` 依赖。
- 已执行真实构建验证。
- 若命中代码问题，已做最小修复并复测。
- 本轮目标、TODO、执行记录、验证结果、复盘全部写入 `docs\dev-records`。

## 验证方式
- `npm install` 或锁文件兼容的本地安装命令
- `bun run build`
- 必要时主仓库回归

## TODO
1. 在 `ui-ux-pro-max-0.1.0\cli` 安装依赖。
2. 执行 `bun run build`。
3. 若失败，判断是环境问题还是代码问题。
4. 若是代码问题，做最小修复并复测。
5. 回写执行记录、验证结果、复盘。
6. 若有代码改动，提交 Git。

## 执行顺序
1. 安装依赖。
2. 构建验证。
3. 问题归因。
4. 必要修复与复测。
5. 文档回写。
6. Git 提交。

## 执行记录
- 已完成 TODO 1：
  - 已在 `ui-ux-pro-max-0.1.0\cli` 执行依赖安装：
    - `npm install`
    - `bun install`
- 已完成 TODO 2：
  - 已执行真实构建验证：
    - `bun run build`
- 已完成 TODO 3：
  - 首次构建失败时已确认：
    - `node_modules` 中依赖已存在
    - 失败根因不是代码缺陷，而是当前 Bun 解析环境未初始化
  - 在执行 `bun install` 后重新构建通过，说明问题属于环境准备缺失，而不是源代码回归问题。
- 已完成 TODO 4：
  - 本轮无需新增代码修复。
  - 当前最小结论是：
    - `extract.ts` 的参数化调用改动未破坏该子工程构建
    - 插件子工程在完成 Bun 环境准备后可正常构建
- 已完成 TODO 5：
  - 已回写执行记录、验证结果与复盘。
- TODO 6 待执行：
  - 待本轮 Git 提交。

## 验证结果
- 子工程依赖安装完成：
  - `npm install`
  - `bun install`
- 子工程构建通过：
  - `bun run build`

## 复盘
- 新问题：
  - 已确认上一轮构建失败是环境准备问题，不是代码问题。
- 边界条件：
  - 该子工程构建依赖 Bun 自己的安装状态，单纯 `npm install` 不足以保证 `bun build` 成功。
- 遗漏点：
  - 当前没有进一步验证 CLI 运行时行为，只验证了安装和构建通过。
- 残余风险：
  - 当前插件子工程已完成依赖扫描、命令执行路径收敛和真实构建验证，剩余风险更多在运行时交互层，而不是当前静态结构层。
- 是否回写规划：
  - 是。本轮已回写构建验证结论。

## 当前结果
- 第二十三阶段已完成：
  - 插件子工程真实构建验证通过
  - 已确认参数化命令执行改动未引入构建回归

## 下一阶段建议 TODO
1. 若继续插件治理，可补 CLI 运行时最小 smoke 验证。
2. 若回到主仓库安全主线，可继续更系统化的 `Host / Origin` 组合矩阵。
3. 若准备收口，可统一提交本轮插件构建验证与报告更新。
