# 护眼模式顶部用户区与主题颜色说明计划

## 目标
- 修正护眼模式下 `topbar-account` 仍然发灰的问题。
- 将模型选择框更明确地贴到 AI 回复条左侧留白区的左上角。
- 整理当前三套主题的主色方向，便于后续按主题切换不同 Logo。

## 范围
- 仅处理顶部用户区、聊天模型框位置和主题颜色说明。
- 修改范围限定在：
  - `public\css\style.css`
- 不改后端，不改聊天逻辑，不改其他页面结构。

## 假设
- 用户当前要的是精确修正，不是继续大范围重做。
- 主题颜色说明以当前 CSS 变量为准。

## TODO
1. 补齐 `paper` 主题下 `topbar-account` 的专属覆盖。
2. 将模型选择框再向 AI 回复条左侧留白区对齐。
3. 执行最小回归。
4. 输出主题色说明。

## 执行记录
- 2026-04-24 已完成：
  - 为 `paper` 主题下的 `topbar-account`、`topbar-login-button`、账号操作按钮补上纸感底色、边框和 hover 覆盖
  - 将模型选择框进一步向 AI 回复列左侧留白区对齐
  - 保留中等桌面宽度和移动端的回退规则

## 验证结果
- 已执行：
  - `node test-page-markup.js`
  - `node test-frontend-state.js`
- 结果：
  - 页面标记测试通过
  - 前端状态测试通过

## 复盘
- 这次两个问题都属于 `paper` 主题和桌面宽屏定位的细节覆盖问题。
- 当前结论：
  - 护眼模式下顶部账号区不应再是灰色，而会回到淡黄纸感
  - 模型选择框相较之前更贴近 AI 回复列左侧留白区
