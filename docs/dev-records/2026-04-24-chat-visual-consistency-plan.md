# AI 对话页视觉一致性统一计划

## 目标
- 继续收敛 AI 对话页视觉细节。
- 统一聊天页的字体层级、圆角和按钮尺寸节奏。
- 在不改变当前布局结构的前提下，让页面整体更完整、更像一个成熟对话产品。

## 范围
- 仅处理 AI 对话页相关样式。
- 修改范围限定在：
  - `public\css\style.css`
  - 如有必要：`test-page-markup.js`
- 不改后端、不改聊天逻辑、不改其他功能页。

## 假设
- 用户说“继续”，表示接受当前方向，希望继续精修整体视觉完成度。
- 当前最值得继续统一的是聊天页内部的视觉节奏，而不是再改结构。

## 风险
- 如果统一过度，可能影响当前已经稳定的移动端适配。
- 如果按钮和消息气泡圆角调整过大，可能破坏现有层级平衡。

## TODO
1. 审阅聊天页字体层级、圆角与按钮尺寸现状。
2. 统一聊天主区、侧栏和 composer 中的关键文字层级。
3. 统一按钮、气泡、卡片的圆角与高度节奏。
4. 保持当前布局和交互不变，只做视觉一致性收敛。
5. 执行前端相关验证，并回写执行记录、验证结果与复盘。

## 完成标准
- 聊天页的标题、正文、辅助文字层级更统一。
- 按钮、消息气泡、侧栏卡片与 composer 的圆角和尺寸更协调。
- 整体视觉更完整，但不引入新的布局变化。

## 验证方式
- 执行：
  - `node --check public\js\app.js`
  - `node test-page-markup.js`
  - `node test-frontend-state.js`

## 执行记录
- 2026-04-24 已复查聊天页的视觉尺度，确认当前最需要统一的是：
  - 消息气泡圆角与阴影
  - composer、输入行和按钮圆角
  - 侧栏按钮、卡片和管理按钮的圆角体系
  - 正文与辅助文字的字号、间距节奏
- 已完成改动：
  - 统一消息气泡圆角，主消息气泡改为更一致的 18px 节奏。
  - 收轻聊天机器人消息卡阴影，减少厚重感。
  - 微收正文行距与段间距，让消息阅读更整齐。
  - 统一侧栏头部按钮、搜索框、会话卡片、归档卡片和管理按钮的圆角层级。
  - 统一 composer 外壳、模型下拉、输入主行、发送按钮和停止按钮的圆角与高度节奏。
  - 统一部分辅助文字字号，让标题、正文、次级信息更成体系。

## 验证结果
- 已执行验证：
  - `node --check public\js\app.js`
  - `node test-page-markup.js`
  - `node test-frontend-state.js`
- 验证结果：
  - 前端脚本语法检查通过。
  - 页面标记测试通过。
  - 前端状态测试通过。
  - 静态核查已确认聊天页核心组件的圆角、按钮高度和字体层级已比上一轮更统一。

## 复盘
- 这轮属于最后一层“手感统一”，没有再动布局结构，所以风险低但收益明显。
- 现在聊天页在视觉上已经更像一个完整产品，而不是多个优化点拼在一起：
  - 圆角体系更统一
  - 按钮尺寸更一致
  - 消息与侧栏的文字节奏更协调
- 新问题归类：
  - 当前必修：无新的阻断问题。
  - 可延后：如果还要继续精修，下一步适合做真实浏览器下的截图验收和细颗粒对比度校正。
