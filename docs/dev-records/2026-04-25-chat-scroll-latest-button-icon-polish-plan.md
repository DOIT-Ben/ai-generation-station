# 2026-04-25 Chat Scroll Latest Button Icon Polish Plan

## 目标
- 将“回到最新回复”按钮里的文本箭头替换为更稳定、更精致的图形箭头，提升控件质感。

## 范围
- `public\index.html`
- `public\css\style.css`
- 与按钮图标结构直接相关的最小测试

## 假设
- 当前按钮的结构、位置和状态表达已经可用，本轮只做图标层打磨。
- 不引入新的图标库，直接使用内联 SVG 即可满足当前项目需求。

## 风险
- 如果图标尺寸与容器比例不协调，按钮会显得头重脚轻。
- 如果 SVG 继承色值不正确，三种主题下可能出现可读性回退。

## TODO
1. 将按钮箭头替换为内联 SVG。
2. 调整图标容器与 SVG 尺寸、描边和对齐。
3. 更新最小测试并执行验证。
4. 回写执行记录与复盘。

## 完成标准
- 按钮图标在三种主题下都清晰稳定。
- 图标与文案对齐自然，整体观感比文本箭头更成熟。
- 相关测试与回归通过。

## 验证方式
- 最小结构测试。
- 无 live 主回归。
- 浏览器视觉回归。

## 执行记录
- 2026-04-25：已确认本轮仅做按钮图标质感升级，不改按钮行为和状态逻辑。
- 2026-04-25：已将文本箭头替换为内联 SVG，下箭头图形更稳定、精致。
- 2026-04-25：已补充 SVG 尺寸和容器对齐样式，保持三种主题下的视觉稳定。

## 验证结果
- `node test-chat-scroll-latest-button.js`：通过。
- `node test-regression.js --skip-live --skip-browser`：通过。
- `node test-ui-visual.js --port 18825 --launch-server`：通过。

## 复盘
- 新问题：未发现新的功能级问题。
- 边界条件：图形箭头必须继承按钮当前文字色，才能在三种主题和双状态下保持统一。
- 遗漏点：结构从文本图标切换到 SVG 后，需要用最小测试锁定图标存在和尺寸定义，避免后续被误删。
- 规划回写：本轮图标质感升级已完成，无需继续扩展按钮逻辑。
