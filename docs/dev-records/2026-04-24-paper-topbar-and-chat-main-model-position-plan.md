# 护眼模式顶部账号区与聊天主区模型选择框位置修正计划

## 目标
- 修正护眼模式下 `topbar-account` 仍显灰色的问题。
- 将模型选择框从输入区附近移到聊天主区左上角留白位置，更接近 AI 回复列左侧。

## 范围
- 仅处理顶部账号区样式与聊天主区模型选择框的位置。
- 修改范围限定在：
  - `public\index.html`
  - `public\css\style.css`
- 不改后端，不改聊天逻辑，不改其他功能页结构。

## 假设
- 用户现在要的是位置和视觉层级的精确修正，不是新功能。
- “AI 回复条左边的留白那个位置，放在左上角”理解为：模型框属于 `chat-main` 的浮层，而不属于 `chat-composer-shell`。

## 风险
- 若模型框完全脱离 composer，可能需要给聊天消息区增加顶部避让。
- 若护眼模式覆盖不够强，`topbar-account` 仍可能被通用背景覆盖。

## TODO
1. 将模型选择框移到 `chat-main` 内部左上角浮层位置。
2. 调整聊天主区间距，避免模型框与消息区重叠。
3. 为 `paper` 主题下 `topbar-account` 提供更强的纸感背景覆盖。
4. 执行最小回归并回写结果。

## 完成标准
- 护眼模式下顶部账号区不再发灰。
- 模型选择框不在输入框旁边，而是在聊天主区左上角留白处。
- 现有前端测试继续通过。

## 验证方式
- 执行：
  - `node test-page-markup.js`
  - `node test-frontend-state.js`

## 执行记录
- 2026-04-24 已建立本计划，待执行。

## 验证结果
- 待执行。

## 复盘
- 待执行。
