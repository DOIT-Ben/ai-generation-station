# 2026-04-26 安全性测试第二阶段计划

## 目标
- 在上一轮安全网关与输入容错测试基础上，继续补强管理员权限边界与上传入口的安全测试。
- 优先验证匿名访问、普通用户越权、管理员自我降权保护、上传文件类型与体积边界。

## 范围
- 测试脚本：
  - `test-security-gateway.js`
- 后端实现核验：
  - `server\routes\state-admin-routes.js`
  - `server\routes\state-route-helpers.js`
  - `server\routes\local.js`
- 文档：
  - `docs\dev-records`

## 非范围
- 不引入新的安全框架。
- 不做外网扫描、依赖 CVE 扫描或 DoS 测试。
- 不处理与本轮无关的 UI 问题。
- 不修改与本轮无关的测试文件。

## 假设
- 现有 `test-security-gateway.js` 已具备创建临时服务与构造请求的基础能力，适合继续承接本轮测试。
- 管理员权限边界与上传入口多数问题可通过测试先发现，不一定需要本轮新增代码修复。

## 风险
- 管理员接口测试需要登录与 CSRF 前置步骤，若脚本顺序不严谨，容易得到假失败。
- 上传边界测试若直接复用默认 10MB 限制，构造样本会变重，因此需要在临时服务中压低上限。

## 完成标准
- 已验证管理员接口的匿名访问、普通用户越权、自我降权保护至少三类边界。
- 已验证上传入口的匿名拦截、文件类型不匹配、超限体积至少三类边界。
- 若发现问题，完成最小修复并通过回归；若未发现问题，完整记录测试结论与残余风险。
- 本轮目标、TODO、验证结果、执行记录、复盘均写入 `docs\dev-records`。

## 验证方式
- `node test-security-gateway.js`
- `node --check test-security-gateway.js`
- 必要时 `npm test`
- `git diff --check`

## TODO
1. 盘点管理员权限边界与上传入口当前测试缺口。
2. 为管理员权限边界补充安全测试。
3. 为上传入口对抗输入补充安全测试。
4. 运行安全测试并判断是否暴露新问题。
5. 若发现问题，做最小修复并回归。
6. 回写执行记录、验证结果、复盘。
7. 若有代码改动，提交 Git。

## 执行顺序
1. 测试缺口盘点。
2. 管理员边界测试补充。
3. 上传入口测试补充。
4. 执行与修复。
5. 文档回写。
6. Git 提交。

## 执行记录
- 已完成 TODO 1：
  - 已确认上一轮仍未覆盖管理员匿名访问、普通用户越权、自我降权保护。
  - 已确认上一轮仍未覆盖上传入口的匿名请求、扩展名与内容不匹配、超限体积对抗输入。
- 已完成 TODO 2：
  - 在 `test-security-gateway.js` 中补充管理员权限边界测试：
    - 匿名访问 `\api\admin\users`
    - 普通用户访问 `\api\admin\users`
    - 当前登录管理员自我降权保护
    - 管理员创建用户后审计日志记录完整性
- 已完成 TODO 3：
  - 在 `test-security-gateway.js` 中补充上传入口对抗输入测试：
    - 匿名上传拦截
    - 不支持扩展名
    - 扩展名与文件内容不匹配
    - 超限体积上传
- 已完成 TODO 4：
  - 执行新增安全测试与语法检查。
  - 本轮新增用例全部通过，没有打出新的管理员越权或上传入口绕过问题。
- 已完成 TODO 5：
  - 因本轮未发现新的实现缺陷，无需新增业务代码修复。
  - 追加执行 `node test-auth-history.js`，确认认证相关回归仍通过。
- 已完成 TODO 6：
  - 已回写执行记录、验证结果与复盘。
- TODO 7 待执行：
  - 待完成本轮 Git 提交。

## 验证结果
- 语法检查通过：
  - `node --check test-security-gateway.js`
- 安全测试通过：
  - `node test-security-gateway.js`
- 认证回归通过：
  - `node test-auth-history.js`
- 提交前检查通过：
  - `git diff --check`
  - 说明：仅存在 LF -> CRLF 提示，无 diff 错误。

## 复盘
- 新问题：
  - 本轮没有打出新的高风险缺陷，说明上一轮修复后基础安全边界仍然稳定。
- 边界条件：
  - 本轮主要补的是“缺失的安全测试面”，不是继续扩大代码修复范围。
  - 上传入口测试通过临时降低 `MAX_UPLOAD_BYTES` 来做边界验证，避免构造大体积样本。
- 遗漏点：
  - 目前管理员接口仍未覆盖全部业务动作，例如批量操作、邀请重发/撤销的更细粒度组合。
  - 上传入口仍未覆盖更多异常 base64 形态与更多音视频文件签名组合。
- 残余风险：
  - 目前未发现新的管理员越权问题。
  - 目前未发现上传入口的匿名绕过、类型绕过和基础体积绕过问题。
  - 后续仍建议继续补：
    - 邀请与密码重置链的频率限制边界
    - 管理员批量操作的审计日志完整性
    - 更多异常编码输入的统一容错测试
- 是否回写规划：
  - 是。本轮已回写新增测试覆盖面和结论。

## 当前结果
- `test-security-gateway.js` 已新增两组高价值安全测试簇：
  - 管理员权限边界
  - 上传入口对抗输入
- 本轮没有新增业务代码修复，属于“补测试、补覆盖、确认边界”的安全推进。

## 下一阶段建议 TODO
1. 扩展邀请、密码重置、管理员批量动作的频率限制与审计完整性测试。
2. 扩展上传入口更多异常 base64、更多音视频魔数与边界文件尺寸组合测试。
3. 扩展更多异常 Query、Header、Cookie 组合输入的统一容错回归。
